name: Visit saved Marketplace listings

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  visit-listings:
    name: Visit saved listing IDs
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Stop until this legacy workflow is reviewed
        run: |
          echo "This inherited workflow is intentionally disabled pending a security review."
          exit 1
